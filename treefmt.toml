[global]
excludes = [
  ".devenv/**",
  ".direnv/**",
  ".venv/**",
  "venv/**",
  "site/**",
  "**/migrations/**",
  "**/__pycache__/**",
]

[formatter.nix]
command = "nixfmt"
includes = ["*.nix"]

[formatter.ruff-format]
command = "ruff"
options = ["format"]
includes = ["*.py"]
